# Clase 1 - Flask + Login + Blueprint

## Objetivo
Construir la base del sistema con login funcional, sesion y una vista protegida.

## Credenciales demo
- Usuario: `admin`
- Password: `123456`

## Ejecucion
```bash
python -m venv venv
# activar entorno
pip install -r requirements.txt
copy .env.example .env   # en Windows
python run.py
```

Abrir `http://127.0.0.1:5000`.
